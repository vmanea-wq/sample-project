[tool.pylint.main]
py-version = "3.11"
jobs = 0
fail-under = 8.0
ignore-paths = [".venv", "venv", "node_modules"]

[tool.pylint."MESSAGES CONTROL"]
disable = [
  "missing-module-docstring",
  "missing-class-docstring",
  "missing-function-docstring",
]

[tool.pylint."FORMAT"]
max-line-length = 100
